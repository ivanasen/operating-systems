#!/bin/bash

# Извежда информация за потребителите в системата.
# Изпраща поздравително съобщение на всеки от тях,
# който е влязал на повече от един логически терминал./GREAT_USERS/

cat /etc/passwd

who | cut -d ' ' -f 1 | uniq -d | xargs -L 1 -I {} sh -c 'echo hello | write {}'
