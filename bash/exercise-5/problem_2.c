// Да се напише продрама на Си,
// която изведжа на стандартния изход току-що въведената информация.

#include <stdio.h>

int main() {
    char in[100];

    while (1) {
        in[0] = '\0';
        gets(in);
        puts(in);
    }

    return 0;
}
